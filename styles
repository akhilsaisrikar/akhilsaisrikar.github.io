:root{
  --bg:#070707;
  --card: rgba(255,255,255,0.03);
  --glass: rgba(255,255,255,0.02);
  --red:#E31919;
  --muted:#bdbdbd;
  --accent: #ff5a5a;
  --maxw:1200px;
  font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, Arial;
  color:#f6f4ef;
}

/* Reset */
*{box-sizing:border-box;margin:0;padding:0}
html,body{height:100%}
body{
  background: linear-gradient(180deg,#040404 0%, #0b0b0b 100%);
  color:var(--muted); -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale; line-height:1.45;
  -webkit-font-feature-settings: "liga" 1;
  /* FEATURE 18: Custom Cursor */
  cursor: url('images/cursor.png'), auto; /* Replace with actual path to a small, custom cursor image */
}

/* FEATURE 8: Animated Particle Background */
.particle-bg{
  position:fixed;inset:0;z-index:-1;opacity:0.65;
  background: radial-gradient(circle at 50% 120%, rgba(227,25,25,0.1) 0%, rgba(7,7,7,0) 50%);
  animation: bgPulse 20s infinite alternate ease-in-out;
}
@keyframes bgPulse{
  0%{transform:scale(1.0);}
  50%{transform:scale(1.05);}
  100%{transform:scale(1.0);}
}

/* Loader */
.loader{
  position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;flex-direction:column;background:var(--bg);
}
.loader-img{width:210px;height:auto;border-radius:12px;box-shadow:0 22px 48px rgba(0,0,0,0.7);transform-origin:center;animation:loaderPop 900ms ease}
@keyframes loaderPop{0%{transform:scale(.98)}50%{transform:scale(1.02)}100%{transform:scale(1)}}
.music-waves{display:flex;gap:6px;margin-top:14px}
.music-waves .bar{width:6px;height:24px;border-radius:3px;background:var(--red);animation:wave 1s infinite ease-in-out}
.music-waves .b2{animation-delay:0.08s}.music-waves .b3{animation-delay:0.16s}.music-waves .b4{animation-delay:0.24s}.music-waves .b5{animation-delay:0.32s}
@keyframes wave{0%{transform:scaleY(0.45);opacity:0.6}50%{transform:scaleY(1.6);opacity:1}100%{transform:scaleY(0.45);opacity:0.6}}

/* FEATURE 9 & 10: Side Menu (Icon-only & Hover Text) */
.side-menu{position:fixed;right:18px;top:18px;z-index:1200;display:flex;flex-direction:column;gap:8px;align-items:flex-end}
.sm-toggle{background:linear-gradient(180deg,rgba(255,255,255,0.02),rgba(0,0,0,0.1));border:0;padding:10px;border-radius:12px;cursor:pointer;color:var(--muted);backdrop-filter:blur(6px)}
.sm-items{display:flex;flex-direction:column;gap:8px;margin-top:6px;transition:all .3s ease;}
.sm-btn{
  display:flex;align-items:center;gap:8px;background:transparent;border:1px solid rgba(255,255,255,0.03);padding:10px 12px;border-radius:12px;color:var(--muted);cursor:pointer;
  min-width:40px;backdrop-filter:blur(4px);transition:transform .18s,background .18s,min-width .3s,padding .3s;overflow:hidden;
  position:relative; /* for tooltip */
}
.sm-btn i { font-size: 1.1em; }
.sm-btn .btn-text {
  transition: transform .3s, opacity .3s;
  white-space: nowrap;
  opacity: 0;
  width: 0;
}

/* Default state: Minimized (icon-only) */
.side-menu.minimized .sm-items { align-items: flex-end; }
.side-menu.minimized .sm-btn {
  min-width: 40px;
  justify-content: center;
  padding: 10px;
}

/* Hover state: Show text */
.sm-btn:hover{transform:translateY(-6px);background:rgba(255,255,255,0.02)}
.side-menu.minimized .sm-btn:hover {
  min-width: 140px; /* expands width */
  padding: 10px 12px;
}
.side-menu.minimized .sm-btn:hover .btn-text {
  opacity: 1;
  width: auto;
  transform: translateX(0);
}
/* End of Floating Menu */


/* Logo hero */
.logo-hero{display:flex;align-items:center;justify-content:center;padding:28px 12px 6px}
.logo-wrap{width:420px;height:160px;display:flex;align-items:center;justify-content:center}
.zp-logo{max-width:100%;height:100%;object-fit:contain;animation:logoFloat 6s ease-in-out infinite}
@keyframes logoFloat{
  0%{transform:translateY(0) rotate(0) scale(1)}
  50%{transform:translateY(-6px) rotate(-1.2deg) scale(1.02)}
  100%{transform:translateY(0) rotate(0) scale(1)}
}

/* Layout */
main{max-width:var(--maxw);margin:0 auto;padding:18px}
.hero{display:grid;grid-template-columns:1fr 320px 440px;gap:28px;align-items:center;margin-bottom:30px}
@media (max-width:1100px){ .hero{grid-template-columns:1fr 1fr;grid-auto-rows:auto} .hero-right{grid-column:1 / -1} .hero-center{display:none} }
.hero-left{}
.hero-title{font-family:"Playfair Display",serif;font-size:44px;color:var(--red);letter-spacing:-0.6px;margin-bottom:8px}
.hero-sub{color:var(--muted);margin-bottom:14px;font-size:16px}
.host-social-links a { color: var(--muted); font-size: 1.3em; margin-right: 15px; transition: color .2s; }
.host-social-links a:hover { color: var(--accent); }

/* host strip center */
.hero-center .host-strip{display:flex;align-items:center;gap:12px;padding:12px 16px;border-radius:999px;background:linear-gradient(90deg, rgba(227,25,25,0.06), rgba(0,0,0,0.2));box-shadow:0 6px 24px rgba(227,25,25,0.04);border:1px solid rgba(255,255,255,0.02);animation:hostPulse 3s infinite ease-in-out}
@keyframes hostPulse{0%{transform:translateY(0)}50%{transform:translateY(-4px)}100%{transform:translateY(0)}}
.host-photo{width:56px;height:56px;border-radius:999px;object-fit:cover;border:2px solid rgba(255,255,255,0.03)}
.host-text .small{font-size:13px;color:#fff;opacity:0.95}
.host-text .host-by{font-size:14px;color:#ffd7d7; margin-top:2px}

/* FEATURE 13: MODERN PLAYER (GLASS EFFECT) */
.player-card{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.35));padding:14px;border-radius:16px;border:1px solid rgba(255,255,255,0.03);width:100%;max-width:440px;backdrop-filter: blur(6px);transition:transform .28s,opacity .28s,box-shadow .28s}
.modern-player {
  background: rgba(0,0,0,0.4);
  border: 1px solid rgba(255,255,255,0.08);
  box-shadow: 0 16px 32px rgba(0,0,0,0.5), inset 0 0 0 1px rgba(255,255,255,0.05);
}
.pc-top{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:12px}
.pc-left{display:flex;align-items:center;gap:12px}
.pc-guest{width:180px;height:180px;border-radius:12px;object-fit:cover;border:2px solid rgba(255,255,255,0.04)}
.pc-info{min-width:160px}
.pc-title{font-weight:700;color:#fff}
.pc-sub{font-size:13px;color:#d0cfcf;margin-top:4px}
.next-info{font-size:13px;color:#ffc7c7;opacity:0.95}

/* waveform */
.waveform{display:flex;gap:6px;align-items:flex-end;height:46px;margin-bottom:12px}
.waveform .wbar{width:6px;height:10px;background:linear-gradient(180deg,var(--red),#ff6b6b);border-radius:3px;animation:waveSmall 800ms infinite ease-in-out;opacity:0.9}
.waveform .wbar:nth-child(2){animation-delay:0.06s}
.waveform .wbar:nth-child(3){animation-delay:0.12s}
.waveform .wbar:nth-child(4){animation-delay:0.18s}
.waveform .wbar:nth-child(5){animation-delay:0.24s}
.waveform .wbar:nth-child(6){animation-delay:0.30s}
.waveform .wbar:nth-child(7){animation-delay:0.36s}
.waveform .wbar:nth-child(8){animation-delay:0.42s}
.waveform .wbar:nth-child(9){animation-delay:0.48s}
.waveform .wbar:nth-child(10){animation-delay:0.54s}
@keyframes waveSmall{0%{transform:scaleY(.4)}50%{transform:scaleY(1.8)}100%{transform:scaleY(.4)}}
.waveform.paused .wbar{animation-play-state:paused;opacity:0.45;transform:scaleY(.6)}

/* controls */
.pc-controls{display:flex;align-items:center;gap:12px;justify-content:space-between;margin-top:8px;}
.controls-left{display:flex;gap:8px;align-items:center}
.btn-ctrl{
  background:rgba(255,255,255,0.03);color:var(--muted);border:0;padding:8px 10px;border-radius:10px;cursor:pointer;
  transition: background .2s, color .2s;
  position:relative; /* for tooltip */
}
.btn-ctrl:hover { background: rgba(255,80,80,0.1); color: var(--accent); }
.btn-play{
  background:linear-gradient(90deg,var(--red),#b41414);color:#fff;border:0;padding:10px 14px;border-radius:12px;cursor:pointer;font-weight:700;
  transition: transform .15s, box-shadow .15s;
}
.btn-play:hover { transform: scale(1.05); box-shadow: 0 4px 15px rgba(227,25,25,0.4); }

.prog-wrap{flex:1;margin:0 0 12px 0;display:flex;flex-direction:column;gap:6px}
.time-display{font-size:12px;color:#d6d6d6;text-align:right}

/* FEATURE 14: Modern Range/Slider Styling */
input[type=range] {
  -webkit-appearance: none; /* Override default look */
  appearance: none;
  width: 100%;
  height: 6px;
  background: rgba(255,255,255,0.1);
  border-radius: 3px;
  cursor: pointer;
  outline: none;
}
input[type=range]::-webkit-slider-thumb {
  -webkit-appearance: none; /* Override default look */
  appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--red);
  cursor: grab;
  border: 3px solid var(--bg);
  box-shadow: 0 0 5px rgba(227,25,25,0.5);
  transition: transform .15s, background .15s;
}
input[type=range]::-webkit-slider-thumb:hover {
  background: var(--accent);
  transform: scale(1.15);
}

/* FEATURE 5: Transcript Content */
.transcript-content {
  margin-top: 15px;
  padding: 10px;
  background: rgba(255,255,255,0.03);
  border-radius: 8px;
  font-size: 14px;
  color: #ccc;
  max-height: 200px;
  overflow-y: auto;
  opacity: 1;
  transition: opacity .4s, max-height .4s ease-in-out;
}
.transcript-content.hidden {
  max-height: 0;
  opacity: 0;
  padding: 0 10px;
  visibility: hidden;
}

/* FEATURE 7: Search Bar */
.search-bar-wrap {
    margin: 0 auto 30px auto;
    max-width: var(--maxw);
    padding: 0 18px;
    display: flex;
    gap: 10px;
}
.search-input {
    flex-grow: 1;
    padding: 12px 16px;
    border-radius: 12px;
    border: 1px solid rgba(255,255,255,0.04);
    background: rgba(255,255,255,0.01);
    color: var(--muted);
    font-size: 16px;
    transition: border-color .2s, box-shadow .2s;
}
.search-input:focus {
    outline: none;
    border-color: rgba(227,25,25,0.5);
    box-shadow: 0 0 0 2px rgba(227,25,25,0.2);
}
/* End of CSS Part 1 */
/* FEATURE 11: Sliding Navigation for Guests (Guest/Episode Cards) */
.slider-nav {
  display: flex;
  gap: 8px;
}
.guest-slider{display:flex;gap:14px;overflow-x:scroll;padding:14px 6px;scroll-snap-type:x mandatory;scroll-behavior:smooth;will-change:transform}
.guest-card{scroll-snap-align:start;min-width:160px;background:linear-gradient(180deg, rgba(255,255,255,0.015), rgba(0,0,0,0.18));padding:12px;border-radius:12px;border:1px solid rgba(255,255,255,0.03);display:flex;flex-direction:column;align-items:center;gap:8px;transition:transform .18s}
.guest-card:hover{transform:translateY(-6px); box-shadow: 0 12px 20px rgba(0,0,0,0.5);}
.guest-card img{width:120px;height:120px;object-fit:cover;border-radius:10px}
.g-title{font-weight:700;color:#fff}
.g-desc{font-size:13px;color:#cfcfcf}
.g-play{margin-top:6px;background:var(--red);color:#fff;padding:8px 12px;border-radius:10px;cursor:pointer;border:0}

/* episodes row */
.episodes{margin-top:26px}
.episode-row{display:flex;gap:12px;overflow-x:scroll;padding:16px 6px}
.ep-card{min-width:320px;padding:12px;border-radius:12px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.12));display:flex;gap:12px;align-items:center;transition: transform .2s, box-shadow .2s;}
.ep-card:hover { transform: translateY(-3px); box-shadow: 0 6px 12px rgba(0,0,0,0.3); }
.ep-card img{width:120px;height:80px;object-fit:cover;border-radius:8px}
.ep-meta{flex:1}
.ep-actions{display:flex;gap:8px;align-items:center}

/* host section */
.host{display:flex;gap:18px;align-items:center;margin-top:30px;padding:14px;background:rgba(255,255,255,0.01);border-radius:12px}
.host img{width:140px;height:140px;object-fit:cover;border-radius:12px}

/* listen grid */
.listen-grid{display:flex;gap:14px;flex-wrap:wrap;margin-top:12px}
.listen-item{display:inline-flex;align-items:center;gap:10px;padding:10px 14px;border-radius:12px;background:transparent;border:1px solid rgba(255,255,255,0.03);cursor:pointer;transition:transform .18s,box-shadow .18s}
.listen-item img{width:28px;height:28px;object-fit:contain;filter:brightness(1.05)}
.listen-item:hover{transform:translateY(-6px);box-shadow:0 18px 40px rgba(227,25,25,0.06)}

/* FEATURE 12: Modern Contact Form */
.contact{margin-top:26px;padding:18px;background:linear-gradient(180deg, rgba(255,255,255,0.01), rgba(0,0,0,0.12));border-radius:12px}
.guest-form{display:grid;grid-template-columns:1fr 1fr;gap:20px}
.input-group { position: relative; }
.input-group.full-width { grid-column: 1 / -1; }
.guest-form input, .guest-form textarea{
  padding: 18px 10px 6px 10px; /* Space for the floating label */
  border-radius: 10px;
  border: 1px solid rgba(255,255,255,0.04);
  background: transparent;
  color: var(--muted);
  width: 100%;
  transition: border-color .2s, box-shadow .2s;
}
.guest-form textarea{min-height:120px}
.guest-form label {
  position: absolute;
  top: 12px;
  left: 10px;
  color: var(--muted);
  pointer-events: none;
  transition: all 0.2s ease-out;
}
.guest-form input:focus, .guest-form textarea:focus {
  outline: none;
  border-color: var(--red);
  box-shadow: 0 0 0 1px var(--red);
}
.guest-form input:focus + label,
.guest-form input:valid + label,
.guest-form textarea:focus + label,
.guest-form textarea:valid + label {
  top: 4px;
  left: 10px;
  font-size: 10px;
  color: var(--accent);
}
.submit-btn {
  transition: background .2s, transform .2s;
}
.submit-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(227,25,25,0.4);
}


/* FEATURE 13: Modern Mini Player */
.mini-player{
  position:fixed;left:50%;transform:translate(-50%, 18px);bottom:16px;z-index:1300;
  display:flex;align-items:center;gap:16px;padding:12px 18px;border-radius:18px;
  background:linear-gradient(90deg, rgba(0,0,0,0.75), rgba(0,0,0,0.9));
  backdrop-filter: blur(12px);border:1px solid rgba(255,255,255,0.08);
  transition:transform .32s cubic-bezier(.4,0,.2,1),opacity .32s, box-shadow .32s;
  max-width: 600px;
  width: 90%;
}
.modern-mini:hover { box-shadow: 0 10px 25px rgba(0,0,0,0.6); }
.mini-player.hidden{opacity:0;pointer-events:none;transform:translate(-50%, 40px)}
.mini-player img{width:56px;height:56px;border-radius:10px;object-fit:cover}
.mini-meta{min-width:160px}
.mini-controls{display:flex;gap:12px;align-items:center;flex-grow:1;}
.mini-controls input[type=range]{min-width: 100px; height: 4px;}
.mini-controls .btn-lg {
  padding: 10px 14px;
  font-size: 1.2em;
  border-radius: 12px;
  background: linear-gradient(90deg,var(--red),#b41414);
  color: #fff;
}
.mini-controls .icon-btn {
  background: rgba(255,255,255,0.05);
  border: 0;
  color: var(--muted);
  cursor: pointer;
  padding: 8px;
  border-radius: 8px;
  transition: background .2s;
}
.mini-controls .icon-btn:hover { background: rgba(255,80,80,0.1); color: var(--accent); }


/* footer */
.site-footer{margin-top:44px;padding:18px;background:linear-gradient(90deg, rgba(227,25,25,0.04), rgba(0,0,0,0.18));border-top:1px solid rgba(255,255,255,0.02)}
.footer-content{max-width:var(--maxw);margin:0 auto;display:flex;align-items:center;justify-content:space-between;gap:20px}
.footer-logo{width:110px;height:auto;opacity:0.95}

/* buttons */
.btn{display:inline-block;padding:10px 14px;border-radius:10px;border:0;cursor:pointer;background:rgba(255,255,255,0.03);color:var(--muted)}
.btn.primary{background:linear-gradient(90deg,var(--red),#ad1212);color:#fff;font-weight:700}
.btn.outline{border:1px solid rgba(255,255,255,0.04);background:transparent}
.btn.sm{padding:8px 10px;border-radius:8px}

/* small nav */
.nav-sm{background:transparent;border:0;padding:6px 10px;border-radius:8px;cursor:pointer;color:var(--muted);font-size:18px}

/* responsive */
@media (max-width:900px){
  .hero{grid-template-columns:1fr;grid-auto-rows:auto}
  .hero-center{display:flex;justify-content:flex-start;padding-left:6px}
  .player-card{max-width:100%}
  .listen-grid{justify-content:flex-start}
  .guest-slider{padding-bottom:30px}
  .mini-controls{gap: 8px; width: auto;}
  .mini-player{max-width: 95%; width: auto; left: 16px; right: 16px; transform:translate(0, 18px);}
  .mini-player.hidden{transform:translate(0, 40px)}
  .mini-controls input[type=range] { display: none; } /* Hide seek bar on very small screens */
  .mini-meta{min-width: 100px;}
  .search-bar-wrap { margin-bottom: 20px;}
}
/* Visualizer canvases */
.canvas-full{
  width: 100%;
  height: 20px;
  display: block;
  border-radius: 8px;
  background: transparent;
  margin-bottom: 8px;
  box-shadow: inset 0 6px 18px rgba(227,25,25,0.03);
}
.canvas-mini{
  width: 220px;
  height: 36px;
  border-radius: 6px;
  background: transparent;
  margin-right:8px;
}

/* When visualizer disabled, CSS waveform exists; keep slight pause style */
.waveform.paused { opacity: 0.6; filter: grayscale(0.15); }

/* ensure mini player canvas responsive */
@media (max-width:900px){
  .canvas-mini{ width:140px; height:36px; }
}

/* FEATURE 4: Custom Scrollbar Styling (Webkit only) */
.custom-scroll::-webkit-scrollbar {
  height: 6px;
  width: 6px;
}
.custom-scroll::-webkit-scrollbar-track {
  background: rgba(255,255,255,0.05);
  border-radius: 10px;
}
.custom-scroll::-webkit-scrollbar-thumb {
  background: var(--red);
  border-radius: 10px;
  transition: background .2s;
}
.custom-scroll::-webkit-scrollbar-thumb:hover {
  background: var(--accent);
}
/* MISSION & VISION SECTION */
.mission-vision {
    padding: 60px 0;
    max-width: var(--maxw);
    margin: 0 auto;
    width: 100%;
}
.mission-vision .section-head {
    text-align: center;
    margin-bottom: 40px;
}
.mission-vision .section-head h2 {
    font-size: 2.2em;
    margin-bottom: 8px;
    font-family: 'Playfair Display', serif; /* Use bold font for primary titles */
}
.mission-vision .section-sub {
    color: var(--red);
    font-size: 1.1em;
}

/* Grid for the two cards */
.mv-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 30px;
    padding: 0 20px;
}

/* Individual Mission/Vision Card Styling */
.mv-card {
    background: rgba(0,0,0,0.4);
    border: 1px solid rgba(255,255,255,0.05);
    border-radius: 16px;
    padding: 30px;
    transition: box-shadow 0.3s ease;
}
.mv-card:hover {
    box-shadow: 0 10px 20px rgba(0,0,0,0.4), 0 0 0 1px rgba(255,255,255,0.1);
}
.mv-card h3 {
    color: var(--red);
    font-size: 1.5em;
    margin-bottom: 12px;
    font-family: 'Playfair Display', serif;
}
.mv-card p {
    color: var(--muted);
    line-height: 1.6;
    margin-bottom: 20px;
}
.mv-card p strong {
    color: #fff;
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .mv-grid {
        grid-template-columns: 1fr;
        padding: 0 16px;
    }
    .mission-vision {
        padding: 40px 0;
    }
    .mission-vision .section-head {
        margin-bottom: 25px;
    }
}
/* ABOUT THE BRAND SECTION */
.about-brand {
    padding: 60px 0;
    max-width: var(--maxw);
    margin: 0 auto;
    width: 100%;
    /* Subtle background to differentiate from adjacent sections if needed */
    background: rgba(0, 0, 0, 0.1); 
    border-top: 1px solid rgba(255, 255, 255, 0.05);
}

.about-content {
    padding: 0 20px;
    max-width: 900px; /* Constrain text width for readability */
    margin: 0 auto;
    text-align: center;
}

.about-content .section-title {
    font-size: 2.5em;
    margin-bottom: 20px;
    font-family: 'Playfair Display', serif; /* Use a bold, striking font */
    color: var(--red) !important; /* Emphasize the section title with brand red */
}

/* Brand Pillars (The Podcast, The Space, The Movement) */
.brand-pillars {
    margin-bottom: 30px;
    padding: 15px 0;
    border-top: 2px solid var(--red);
    border-bottom: 2px solid var(--red);
    display: inline-block;
}

.pillar-heading {
    font-size: 1.2em;
    color: var(--muted);
    margin-bottom: 10px;
    text-transform: uppercase;
    letter-spacing: 2px;
}

.brand-pillars ul {
    list-style: none;
    padding: 0;
    display: flex;
    justify-content: center;
    gap: 20px;
}

.brand-pillars li {
    color: #fff;
    font-size: 1.1em;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 1px;
}

/* Main Brand Description */
.brand-description {
    font-size: 1.4em;
    line-height: 1.7;
    color: var(--muted);
    margin-bottom: 25px;
}

.brand-description strong {
    color: #fff;
    font-weight: 700;
}

/* Supporting text */
.brand-discovery {
    font-size: 1em;
    color: var(--muted);
    font-style: italic;
}


/* Responsive adjustments */
@media (max-width: 600px) {
    .about-content {
        padding: 0 16px;
    }
    .about-content .section-title {
        font-size: 2em;
    }
    .brand-description {
        font-size: 1.2em;
    }
    .brand-pillars ul {
        flex-direction: column;
        gap: 8px;
    }
}
/* ABOUT HOST SECTION */
.about-host {
    padding: 80px 20px;
    max-width: var(--maxw);
    margin: 0 auto;
    width: 100%;
    /* Grid layout for two columns: 1 part for image, 2 parts for bio */
    display: grid;
    grid-template-columns: 1fr 2fr; 
    gap: 40px;
    align-items: center;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
}

.host-image-wrap {
    text-align: center;
}

.host-photo-lg {
    width: 100%;
    max-width: 300px; 
    height: auto;
    border-radius: 12px;
    object-fit: cover;
    border: 3px solid var(--red); /* Framed by the brand red color */
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
}

.host-name {
    font-size: 2.5em;
    color: #fff;
    margin-bottom: 5px;
    font-family: 'Playfair Display', serif;
}

.host-designation {
    font-size: 1.1em;
    color: var(--red);
    margin-bottom: 20px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.host-social-links.lg {
    margin-bottom: 30px;
}

.host-social-links.lg .social-link {
    font-size: 1.5em;
    margin-right: 15px;
    color: var(--muted);
    transition: color 0.2s;
}

.host-social-links.lg .social-link:hover {
    color: var(--accent);
}

.host-bio-text p {
    margin-bottom: 15px;
    line-height: 1.7;
    color: var(--muted);
}

.host-contact-btn {
    margin-top: 20px;
}

/* Responsive adjustments: Stack columns on smaller screens */
@media (max-width: 900px) {
    .about-host {
        grid-template-columns: 1fr;
        gap: 30px;
        padding: 60px 20px;
        text-align: center; /* Center content when stacked */
    }
    .host-image-wrap {
        order: -1; /* Move image to the top */
    }
    .host-photo-lg {
        max-width: 200px;
    }
    .host-bio-content {
        padding: 0;
    }
    .host-social-links.lg {
        display: flex; /* Ensure social links are still inline */
        justify-content: center;
    }
}
/* VOICE MESSAGE SECTION */
.voice-message {
    padding: 60px 20px;
    max-width: 700px; /* Keep this form section slightly narrower for focus */
    margin: 0 auto;
    text-align: center;
}

.voice-message h2 {
    font-size: 2.2em;
    margin-bottom: 8px;
    font-family: 'Playfair Display', serif;
}

.vm-sub {
    color: var(--muted);
    margin-bottom: 30px;
    font-size: 1.1em;
}

.voice-form {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    background: rgba(0, 0, 0, 0.4);
    padding: 30px;
    border-radius: 16px;
    border: 1px solid rgba(255, 255, 255, 0.05);
}

/* Record Button Area */
.record-wrap {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 15px;
    min-height: 50px; /* Ensure consistent height */
}

.record-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    flex-grow: 1;
    padding: 12px 20px;
    font-size: 1.1em;
    transition: background 0.3s, transform 0.1s;
}

.record-btn:hover {
    transform: scale(1.02);
}

.record-timer {
    background: rgba(255, 255, 255, 0.05);
    color: var(--red);
    padding: 10px 15px;
    border-radius: 8px;
    font-weight: 700;
    min-width: 60px;
}

/* Active Recording State (Optional: requires JS) */
.record-btn.active {
    background: linear-gradient(90deg, #b30000, #ff5a5a);
    box-shadow: 0 0 10px rgba(255, 0, 0, 0.6);
    animation: pulse 1.5s infinite;
}

@keyframes pulse {
    0% { opacity: 1; }
    50% { opacity: 0.7; }
    100% { opacity: 1; }
}

/* Responsive adjustments */
@media (max-width: 600px) {
    .voice-form {
        grid-template-columns: 1fr;
    }
    .record-wrap {
        flex-direction: column;
        align-items: stretch;
    }
    .record-btn, .record-timer, .submit-voice-btn {
        width: 100%;
        margin-bottom: 5px;
    }
}
/* Update the .listen-item for a rectangular, logo-only container */
/* --- Listen Section Styling --- */
#listen {
  max-width: var(--maxw);
  margin: 40px auto 40px; 
  padding: 0 20px;
}

#listen .section-head {
  padding-left: 0;
  margin-bottom: 30px;
  font-size: 2.5rem; /* This is the standard size you were using */
  text-align: center;
  font-family: 'Playfair Display', serif; */
}

/* Ensure the logo styles remain aggressive to fill the box */
.listen-item {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 160px; /* Rectangular width */
  height: 80px; /* Rectangular height */
  background: var(--glass);
  border-radius: 12px;
  transition: transform 0.2s ease;
  cursor: pointer;
}

.listen-item img {
  width: 100%; /* Force image width to 100% of the box */
  height: 100%; /* Force image height to 100% of the box */
  object-fit: fit; /* Stretches/fills the image to the dimensions */
  opacity: 1; 
}
